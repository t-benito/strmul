[package]
name = "strmul"
version = "0.1.2"
edition = "2024"
repository = "https://github.com/MrMozzarella-d/strmul"
license = "MIT"
description = "CLI-tool to multiply strings"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }

[profile.dist]
inherits = "release"
lto = "thin"
